# Implementation Plan

## 目標

整理 `GDD/貪食蛇GDD.md` 的 UI 圖片配置與重複規格：

1. ch2.2 在對應小標下插入對應示意圖。
2. ch2.2 刪除「相對方位標示」。
3. ch2.2 刪除「HUD 介面」，並把 ch1.2 缺少的 HUD 說明補齊。
4. ch3.3 插圖改為 `Ref_對局中.png`。
5. ch3 適合位置插入 `Ref_對局結算.jpg` 與 `Ref_平台結算.png`。

## 執行前確認

狀態：等待使用者確認

## 預計修改

1. 備份 `GDD/貪食蛇GDD.md`。
2. 檢查圖片存在：
   - `GDD/Reference Image/Ref_大廳.png`
   - `GDD/Reference Image/Ref_對局中.png`
   - `GDD/Reference Image/Ref_對局結算.jpg`
   - `GDD/Reference Image/Ref_平台結算.png`
3. ch2.2：
   - 在「遊戲大廳」描述下插入 `Ref_大廳.png`。
   - 檢查 ch2.2 目前有哪些小標與圖片，將圖片放到對應小標底下。
   - 刪除 `* **相對方位標示**...`
   - 刪除 `* **HUD 介面**...`
4. ch1.2：
   - 補上從 ch2.2 移除的 HUD 重要資訊：
     - 無小地圖
     - 右上局內排行榜 4 行、No.1 皇冠
     - 正上方擊殺橫幅
     - 右下 Dash 與暴食按鈕
     - 左下虛擬搖桿
     - 中下高級道具欄
     - 畫面四周 crown icon 指向第一名
   - 保留現有 `Ref_對局中.png` 圖片。
5. ch3.3：
   - 將目前第一名方位指示插圖替換為 `Ref_對局中.png`。
6. ch3 結算相關位置：
   - 在 ch3.4 結算評價與獎勵機制插入 `Ref_對局結算.jpg`。
   - 在 ch3.4.6 平台結算與養成機制插入 `Ref_平台結算.png`。
7. 移除 ch2.2.4 / ch2.2.5 中若已搬到 ch3 的結算圖片，避免重複。
8. 檢查圖片路徑、章節結構與 details 收合區塊。

## 不修改範圍

- 不修改圖片檔案。
- 不修改 ch14 好友房流程。
- 不重寫 ch3 結算規則數值。
- 不修改 Mermaid 流程圖內容，除非圖片搬移造成空章節需清理。

## 預期輸出

- 更新後的 `GDD/貪食蛇GDD.md`
- 修改前備份路徑
- 簡短回報各圖片移動與刪除項目
