# Current Implementation Plan

## 任務

Review `GDD/貪食蛇GDD.md` 的 ch7 / ch8，補充地圖道具與高級道具啟用後的 Buff 顯示規格，並優化章節寫法，使規格更簡單、易讀、好理解。

## 需求重點

1. 地圖道具與高級道具啟用效果後，介面上方會有對應的顯示說明。
2. 插入圖片 `GDD/Reference Image/Ref_對局中.png`，輔助說明 Buff 顯示位置與資訊。
3. 補充細節：
   - 啟用效果時，Buff 列會有演出。
   - 覆蓋效果時，Buff icon 會有特別演出。
4. Review ch7 和 ch8，判斷是否需要優化寫法或重新分配章節。

## 影響範圍

- 修改檔案：`GDD/貪食蛇GDD.md`
- 影響章節：
  - ch7 食物、地圖道具與積分系統
  - ch8 局內道具與覆蓋規則
- 參考圖片：`GDD/Reference Image/Ref_對局中.png`
- 不修改圖片。
- 不修改程式碼。

## 將讀取的檔案

- `GDD/貪食蛇GDD.md`
- `GDD/Reference Image/Ref_對局中.png` 是否存在

## 將修改的檔案

- `GDD/貪食蛇GDD.md`

## 備份策略

- 修改前備份原檔到 `scratch/backups/YYYYMMDD_HHMMSS/GDD/貪食蛇GDD.md`。

## 執行步驟

1. 備份 `GDD/貪食蛇GDD.md`。
2. 確認 `Ref_對局中.png` 存在。
3. 讀取 ch7 / ch8 完整上下文。
4. Review 章節結構，判斷最佳改寫方式。
5. 優先採取低風險重整：
   - ch7 保留地圖道具來源、拾取、生效基礎規格。
   - ch8 集中描述局內道具使用、Buff 顯示、覆蓋、重複使用、按鈕狀態。
6. 在 ch8 補充「Buff 顯示列」規格，包含：
   - 顯示位置：介面上方。
   - 顯示資訊：道具 icon、名稱/簡短說明、剩餘時間或狀態。
   - 啟用演出：Buff 列滑入/亮起/脈衝。
   - 覆蓋演出：Buff icon 閃光、縮放或刷新動畫。
   - 地圖道具與高級道具共用同一 Buff 顯示列。
7. 插入 `Ref_對局中.png` 作為位置輔助圖。
8. 若 ch7/ch8 有重複描述，保留一處主規格，另一處改為引用。
9. 讀回 ch7/ch8 驗證。
10. 回報變更摘要與備份位置。

## 驗證方式

- ch8 明確包含 Buff 顯示列規格。
- ch8 明確說明地圖道具與高級道具啟用後都會顯示。
- ch8 明確包含啟用演出與覆蓋演出。
- ch8 插入 `Ref_對局中.png`。
- ch7/ch8 結構比原本更少重複、更易讀。
- 不修改圖片或程式碼。

## 不做的事

- 不改數值平衡。
- 不修改程式碼。
- 不新增圖片。
- 不刪除既有核心規格，只做整理、補充與引用。

## 等待確認

請使用者回覆「確認」或「開始」後再執行。
