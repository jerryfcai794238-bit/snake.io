# Current Plan

## 目標

將 `GDD/貪食蛇GDD.md` 的 `ch5.4` 改寫成「死亡總述 + 兩個子章節」的結構：先說明蛇隻死亡後頭部掉落 `OOO`、身體轉化為殘骸結晶，且兩者都保留 `10 秒`，再分別在 `5.4.1` 與 `5.4.2` 展開各自規格。

## 等待確認狀態

- 等待你回覆 `開始`

## 預計修改檔案

- `GDD/貪食蛇GDD.md`

## 預計步驟

1. 備份 `GDD/貪食蛇GDD.md` 到 `scratch/backups/YYYYMMDD_HHMMSS/GDD/貪食蛇GDD.md`
2. 重寫 `ch5.4` 的總述段，避免重複描述
3. 讓 `5.4.1 殘骸結晶` 只保留殘骸規格
4. 讓 `5.4.2 道具` 只保留道具規格與掉落規則
5. 檢查 `10 秒` 保留規則是否在兩處都有對應但不重複冗述

## 預期輸出

- `ch5.4` 結構改成總述 + 兩個子章節
- 規格更清楚，避免重複內容

## 可能影響範圍

- 只影響死亡與掉落相關段落
- 不更新版本紀錄
