# 專案資料夾整理計畫

## 目標

整理專案根目錄，將與 `snake.io` 主專案無關的檔案（如泡泡龍相關文件）以及未歸類的會議反饋檔案歸檔，保持專案根目錄乾淨。

## 等待確認狀態

等待使用者回覆「開始」以執行本計畫。

## 預計修改/移動檔案

- `泡泡龍GDD.md` -> 移動至新建的 `GDD_泡泡龍/` 資料夾（或 `GDD/泡泡龍/`）
- `泡泡龍美術需求GPT.md` -> 移動至新建的 `GDD_泡泡龍/` 資料夾（或 `GDD/泡泡龍/`）
- `提案會議反饋.txt` -> 移動至 `GDD/提案會議反饋.txt`

## 修改前備份

在移動規格與 GDD 相關文件前，依據 `AGENTS.md` 規範，先備份至：
- `scratch/backups/20260618_175519/泡泡龍GDD.md`
- `scratch/backups/20260618_175519/泡泡龍美術需求GPT.md`
- `scratch/backups/20260618_175519/提案會議反饋.txt`

## 預計步驟

1. 備份要移動的規格文件。
2. 建立新資料夾 `GDD_泡泡龍/`（若使用者偏好，亦可放於 `GDD/泡泡龍/`，本計畫預設建立 `GDD_泡泡龍/` 以作專案區隔）。
3. 將 `泡泡龍GDD.md` 與 `泡泡龍美術需求GPT.md` 移動至 `GDD_泡泡龍/`。
4. 將 `提案會議反饋.txt` 移動至 `GDD/` 資料夾下。
5. 檢查並確認專案根目錄已整理乾淨，且無檔案損壞。

## 預期輸出

- 根目錄不再有未歸類的 `.md` 與 `.txt` 規格文件。
- 新增 `GDD_泡泡龍/` 資料夾，內含：
  - `泡泡龍GDD.md`
  - `泡泡龍美術需求GPT.md`
- `GDD/` 資料夾下新增：
  - `提案會議反饋.txt`

## 可能影響範圍

- 僅移動文件位置，不修改任何文件內容或代碼邏輯。
